{{>layouts/header}}

    <div>
        {{# id}}
            <h1>{{id}} {{username}} Hello! </h1>
        {{/ id}}

        <!-- id 가 없을 때 -->
        {{^ id}}
            <h1>{{username}} Hello! </h1>
        {{/ id}}
    </div>

    <!-- 버튼 -->
    <button type="button" class="btn btn-light">Button</button>

{{>layouts/footer}}

